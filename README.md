# AnkiMorphs Mecab Korean

This is a companion add-on for AnkiMorphs, which imports a Korean MeCab morphemizer.

## Installation

```zsh
# Download dependencies
uv sync
# Test
uv run pytest
# Create a symlink in your anki addons directory
uv run scripts/install_addon.py
```

